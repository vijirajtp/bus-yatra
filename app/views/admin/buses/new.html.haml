.app-content-header
  .container-fluid
    .row
      .col-sm-6
        %h3.mb-0 Add New Bus
.app-content
  .container-fluid
    .row
      .col-sm-6
        .card
          .card-body
            = form_with model: [:admin, @bus], local: true do |f|
              = render "shared/error_messages", object: f.object
              = render "form", f: f
              = f.submit "Create Bus", class: "btn btn-primary"
              = link_to "Back", admin_buses_path, class: "btn btn-secondary"